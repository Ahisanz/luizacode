'''
while => {

}
Listas => {
    list_B[0] + list_B[1] = concatenação em string
    usa o elemento de uma lista.
    se for int vai somar o valores.

    cursos.pop()
    tira o ultimo da lista e te retorna
    não exige atributo, default tira o ulitmo, mas pode colocar o index e tirar o comandado

    cursos.reverse()
    reverte a ordem dos elementos.

    cursos.sort()
    orgniza em ordem , e ordem alfabetica e numero crescente
    para fazer decrescente: cursos.sort(reverse=True)

    função sorted()
    deve ser colocado dentro de uma variavel
    sorted(cursos, reverse=True)

    impares.count()
    conta quantos elementos tem iguais ao atribuido

    cursos.index()
    coloca o texto e te devolve o posicionamento daquele elemento na lista.

    len(cursos)
    conta quantos elementos tem dentro de lista.

    list_to_str = ''.join(cursos)
    primero padrao para juntar

    str_to_list = list_to_str.split('')
    coloca devolta em lista

    cursos.clear()
    remove todos os elementos.

    for (index, curso) in enumerate(cursos)
    (0, 'arte)
    pode dizer em qual numero que voce quer que comece

    funçoes numericas
    min(impares)
    max(impares)
    sum(impares)

    TUPLES
    igual que lista so que com ()
    usa quando voce quer obrigar a permanencia dos elementos 
    list = let
    tuples = const
}

'''